<div>
    {{bs-notifications class='notifications' showTime='5000'}}

    {{#if session.isAuthenticated}}
        {{partial "navigation"}}
    {{/if}}

    <div class="container">
        {{outlet}}
    </div>
</div>
